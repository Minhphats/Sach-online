@model IEnumerable<Lab03_4.Models.CHUDE>

<div class="list-group">
    @foreach (var item in Model)
    {
        <a href="@Url.Action("SachTheoChuDe", "SachOnline", new { iMaCD = item.MaCD })" class="list-group-item">
            
            @item.TenChuDe
        </a>
    }
</div>

